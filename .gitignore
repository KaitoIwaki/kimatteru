node_modules
app/dist

# ブラウザ版の出力（npm run build:web）。置く先で作るので追跡しない
web/

# cap sync が dist から生成するので追跡しない
app/ios/App/App/public

.DS_Store
*.local

# 署名用の秘密鍵（絶対にコミットしない）
SECRET_*.pem
app/tools/*.p8
*.p8
app/tools/asc.local.json
