# カレンダーアプリ デザイン依頼書（ハンドオフ v0.2）

このドキュメントだけで文脈が完結するように書いています。前提 → 決定済みのルール → 今回デザインしてほしいもの、の順です。

---

## 1. アプリの前提

**コンセプト：「不確定な予定と決まっている予定が一目でわかるカレンダー」**

- 既存カレンダーは確定した予定も「入るかもしれない予定」も同じ見た目で並ぶ。この曖昧さをカレンダー上で可視化するのが核
- 看板画面は月表示。開いた瞬間の見え方が命
- 給料計算（バイト向け）は看板に出さない「隠れた便利」
- **シンプルで綺麗であることを、機能追加より優先する**
- プラットフォーム：iOS想定。ターゲットはバイトをする学生・若年層

## 2. 決定済みのデザインルール（変更しないでほしい土台）

### 2軸の視覚言語

- **色 ＝ 種類**、**塗り／点線 ＝ 確定度** の2軸構造
- 確定 … 色でしっかり塗られた棒（ピル）
- 不確定 … 点線の輪郭＋薄い背景色＋先頭に「？」
- 実績（バイトのみ）… 塗り＋先頭に「✓」、不透明度90%
- 無くなった … 取り消し線＋薄いグレー（削除せず残す）
- 記号は「？」「✓」の文字（図形ではなく）。手書きで「??」を付ける習慣に乗る

### カラー

| 種類 | 色 | 不確定の呼び名 | 確定の呼び名 |
|---|---|---|---|
| バイト | ティール `#1D9E75` | 希望シフト | 確定シフト（＋実績） |
| 用事 | パープル `#534AB7` | まだ分からない用事 | 確定した用事 |
| 遊び | コーラル `#D85A30` | 候補日 | 約束 |

### 画面構成（設計済み）

1. **新規作成**：種類チップ／「この予定は：決まってる・まだ不確定」セグメント（初期値は決まってる）／カレンダーでの見え方プレビュー
2. **月表示（看板）**：7列グリッド＋ピル。上部「給料」トグルで実績が金額表示＋月合計に切り替わる
3. **確認ダイアログ（画面中央）**：全種類共通の骨格「**前進／消滅／保留**」の3択
   - バイト「このシフト、どうなりました？」→ 決まった／無くなった／まだ分からない（時間ステッパー付き、変更あれば「→ 変更あり」）
   - 用事「この用事、どうなりました？」→ 同上
   - 遊び「約束、決まった？」→ 候補日チップから決まった日を選ぶ。残りの候補日は自動で消える
   - 働き終えたシフト「今日は何時まで働いた？」→ ✓記録する（¥金額）／働かなかった／あとで
4. **シフト詳細**：左帯ゲージが希望→確定→実績で満ちる。実績で終了時刻が実時間に更新され、実働と給料が現れる

- 文言はすべて口語（「確定した」ではなく「決まった」）。ダイアログのボタンに取り消し線は使わない

## 3. 今回デザインしてほしいもの（3件・優先順）

背景：機能の核は固まった。バズ・拡散の観点で「共有される瞬間」を製品に組み込みたい。デザインの美しさは前提条件として全体に効かせてほしい。

### 依頼A：「決まった」瞬間の演出（優先度：高／スコープ内）

- ダイアログで「決まった」を押した瞬間、点線ピルが塗りに変わるトランジション
- アプリで一番気持ちいい0.5秒にしたい。人に見せたくなるマイクロインタラクション
- 派手なコンフェッティではなく「シンプルで綺麗」の範囲で。？が消えて✓（または塗り）に変わる過程そのものを演出に
- **§6にこちらのシーケンス案を書いた。この方向を土台に、タイミング・イージング・変形の詳細を磨いて提案してほしい**（より良い案があれば代案も歓迎）

### 依頼B：「今月のまとめ」カード（優先度：高／機能追加小・拡散効果大）

- 月末に自動生成される縦長1枚カード。**そのままInstagramストーリーズ／Xに貼れるスクショ映え**が目的
- 載せる内容：働いた時間と稼いだ額（✓の集計）／果たした約束の数／無くなった予定の数
- Spotifyの年間まとめ・家計簿アプリの月次カードの立ち位置。ただし見た目はこのアプリの視覚言語（？✓、3色、塗り／点線）で
- 縦長（9:16想定）とアプリ内表示の2形態

### 依頼C：候補日の共有リンク・Web回答画面（優先度：中／参考デザインでOK）

- 遊びの候補日を友だちにリンクで送り、**相手はアプリなしで**ブラウザから○×を付けられる
- 決まると送り主のカレンダーで点線→塗りに変わる（依頼Aの演出が発火）
- 調整さん級の摩擦のなさ＋このアプリらしい見た目。アプリの入口になるページなので、世界観の名刺として
- v1に入れるかは未定。方向性が見えるモック1枚で十分

## 4. トーンの指針

- 「シンプルで綺麗」が最優先。装飾で盛るのではなく、余白・タイポグラフィ・動きの質で綺麗にする
- 文言は口語で、予定を入れる瞬間の自分への問いかけとして自然な言葉を選ぶ
- 日常の予定はほとんど塗り。たまにある点線がふっと浮く——この静かな対比を壊さない

## 5. デザインの雰囲気（決定）：「静かな文房具」

ひとことで言うと**きれいに使い込まれた手帳**。このアプリの主役は「たまにある点線がふっと浮く」静かな対比なので、地は徹底的に無口にし、**色は予定のピルだけが持つ**。

- **地とインク**：温かい紙白（#FAF9F4 目安。クリームに寄せすぎない）＋緑がかった墨色（#26251F 目安）。純白・純黒は使わない
- **区切り**：影はほぼ使わず、細い罫線（ヘアライン）で区切る。カードを浮かせない
- **色の規律**：ティール #1D9E75／パープル #534AB7／コーラル #D85A30 の3色は予定ピルと確定度の表現だけに許す。ボタン・アクセント等のUIにこの3色を流用しない（「色＝種類」の軸が濁るため）
- **タイポグラフィ**：本文はクセのないゴシック（ヒラギノ角ゴ／SF）。**数字だけは主役級に**——時刻・日付・金額のアプリなので、等幅数字（tabular）で大きく美しく組む
- **ダークモード必須**：バイト後の夜に見るアプリ。黒背景への単純反転ではなく「深い墨色の紙に点線が浮かぶ」イメージで
- **避ける方向**：ポップ・ゆるキャラ・丸文字／グラデーション・ネオン・ガラス風／業務ツール調の寒色グレー

## 6. モーション指針「静かだけど、触ると気持ちいい」（依頼Aの前提としてこの方向で）

**大原則：普段は動かない。動くのは状態が変わった瞬間だけ。** ただし「動かない」と「反応しない」は別物。静けさは画面全体の話で、指が触れたものは贅沢に応える。気持ちよさは次の4つから作る：

1. **即応** — touch-down から50ms以内に必ず反応する（scale 0.96前後の沈み込み）。紙を指で押さえる感触。気持ちよさの半分は速さ
2. **バネ** — 一方通行の ease-out だけでなく、要所はスプリング。ただし**オーバーシュートは1回だけで素早く減衰**（ぷるぷる揺らさない。文房具は硬い）
3. **ハプティクスの振り付け** — 視覚のビートに同期した多段ハプティクス。目と指で同じリズムを感じさせる
4. **物理メタファー** — ペン・判子・水位・紙。無機質なUIアニメではなく文房具の手触りに寄せる

音は鳴らさない。`Reduce Motion` 時は動きをクロスフェードに置き換える（ハプティクスは残す）。

### 主役：「決まった」の約0.5秒（点線 → 塗り）

ペンでなぞって確定させるイメージ。速さ（合計約0.5秒）は変えずに密度を上げる。

1. **0ms** 押下でボタンとピルがわずかに沈む（scale 0.96、予備動作）＋軽いハプティクス。ダイアログはフェード＋縮小（scale 0.97、150ms）で閉じる
2. **〜100ms** 破線の隙間がシュッと詰まって実線になる。「チッ」という小さな rigid ハプティクス
3. **〜370ms** 塗りが**左から右へマーカーでなぞるように**満ちる（250ms、ease-out）。塗りの先端はわずかに滲む（インクのにじみ）。文字は通過点から白に反転し、先頭の「？」は塗りに押し出されて消える
4. **〜450ms** 満ちた勢いで scale 1.04 まで膨らみ、スプリングで 1.00 に着地（減衰1回）＋成功ハプティクス

### 新規原則：✓は判子で押す

- 実績確定（「✓記録する」）の瞬間、✓バッジが**判子のように上から押される**：scale 1.8 → 1.0、微回転（−10° → 0°）、300ms、重めのハプティクス。日本の文房具文化にそのまま乗る、このアプリ固有の気持ちよさ
- 金額は**オドメーターのように回り上がる**（¥0 → ¥6,160、450ms、ease-out減速）。等幅数字なので桁が揺れない
- 月表示に✓ピルが現れるときも同じスタンプの短縮版（scale 1.2 → 1.0）

### 脇役の動き

- **詳細画面のゲージ**：表示時に水位が満ちる（400ms）。**満ちきった水面がひと揺れ**（小さなオーバーシュート）してから静止。？→✓バッジはスタンプ短縮版
- **給料トグル**：ノブはスプリングで滑る。実績ピルの「✓名前 ⇔ ¥金額」は数字が桁ごとにオドメーター回転。月合計はカウントアップし、**着地の瞬間に軽いハプティクス**
- **月めくり**：スワイプにラバーバンドの弾性。端まで引くと紙の抵抗感
- **「無くなった」**：ここだけは動きを増やさない。彩度が静かに抜け、取り消し線が左から引かれる（200ms）。哀悼のトーン
- **「まだ分からない」**：ダイアログが閉じるだけ。点線は動かない（保留は無音）
- **まとめカード**：項目が上から順にフェードイン（各40msずらし）、**最後に合計金額だけ判子でスタンプ**される
- **画面遷移**：iOS標準のプッシュ遷移のまま。独自遷移は作らない

静と動の対比が気持ちよさの源泉。全部を動かすと全部が死ぬので、「決まった」「✓」「金額」の3つに動きの予算を集中投資する。
