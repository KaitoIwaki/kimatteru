# 次にやること

**2026-08-22、v1.0 (87) を公開した（`READY_FOR_SALE`）。**
https://apps.apple.com/jp/app/id6794792375

手元のコードは v0.29.0。ここから先の順番を決めて置いておく。

状態はいつでもこれで読める。

```
cd app && node tools/asc.mjs status
```

---

## 0. 公開した直後にやること

- [x] 審査を通った。課金3つも `APPROVED`
- [x] 公開した（`READY_FOR_SALE`）
- [x] **本番で商品が取れることを確かめた。**（2026-08-22）
      設定 → 応援 に3つの行が出て、値段も ¥300 / ¥600 / ¥1,000 と円で出た。
      商品が取れないと群ごと隠す作りなので、ここが駄目だと審査メモに書いた
      場所に何も無い状態になっていた。**一番大きな risk は越えた**
- [ ] （急がない）**自分で ¥300 を1回買う。**
      買って初めて分かるのは「決済が最後まで通るか」だけ。
      支払いシート・パスワード・レシートの流れは、まだ一度も通っていない。
      ¥300 のうち ¥210 は戻るので、実質の負担は90円ほど
- [ ] **ウィジェットを置き直す。** App Store から入れ直した版で、小・中・大の3つ
- [ ] しばらく自分で使う。**何を直すべきかは、使っていると出てくる。**
      1.1 の中身を決めるのはそのあとでいい

---

## 1. 次の版（v1.1）に入れるもの

前の版から積み残していて、**中身が決まっているもの**だけ。

### 1-1. 実績の画面の詰まり
時間の話が5か所に散っている（実際の時刻・変更あり・希望・休憩・実働時間）。
互いに計算で出せる関係なのに、3通りの言い方で書いている。

→ **案1「金額を主役に」**（`tools/jisseki-card.cjs` で描いた案）。
金額を34pxにして、根拠を下に2行で畳む。カードの中身が8行から3行になる。

あわせて、**「働いた時間を直す」を緑のボタンから「···」の行へ降ろす。**
緑は「その予定が次へ進む」ための操作の色。実績を直すのは次へ進む操作ではない。

### 1-2. アイコン
型押しが浅く、実際に出る大きさでのコントラストが **1.38〜1.48** しかない。
大きな図形でも 3.0 は欲しい。App Store の検索結果は白地なので、
**一番見られる場所で一番弱い**。

→ 型押しを深くする（2.5〜3.0）。あわせて **丸のいくつかを点線の輪にする**。
いまは12個とも同じ形で、どのカレンダーアプリでも使える絵になっている。
塗りと点線を入れれば、このアプリだけの絵になる。

測り直しは `tools/make-icon.mjs` の出力で見る。

### 1-3. 「使い始める前の額」
今年の累計に、手で入れる初期値。アプリを年の途中から使い始めた人の
合計が実際と合わない。

---

## 2. 配色（A は入れた。B は残っている）

**2026-08-22、測って分かったこと。壊れていたのは色ではなく、色の作り方だった。**

`softFill` も `paper` も `inkOn` も、地の明暗を見ていない。明るい方のピルが
そのまま暗い地に乗っているので、まだの紙が中間の明るさになって塗りに寄る。
**いまの色のままでも 1.22 → 1.45 まで戻る。**

### A. 作り方を地に合わせる ＋ 冷たい地　— [x] 入れた（v0.29.0、2026-09-03）

決めたのは「A ＋ 冷たい地」。色そのものは替えず、作り方と地だけ直した。

```
                      面と地    面の上の字   まだの字
いまのダークモード        1.22        —          —
A ＋ 冷たい地（入れた）   1.51      9.93       16.04   ← 実機の DOM で測った
B（Lab の色・まだ）       1.48     10.10          —
```

入れた中身。

- `dark()` を足し、`softFill` / `softLine` / `inkOn` が地の明暗を見るようにした。
  暗いときは白ではなく `DARK_CELL #12151A` へ寄せる（面は色を24%だけ残す）
- `paperShow()` を足した。**暗いときは、まだのピルに面を敷かない**（`transparent`）。
  点線の枠だけが色を持つ。塗り＝面あり／まだ＝面なし、で二つが離れる
- `inkDash()` を足した。暗いときのまだの字は色に白を88%混ぜてほぼ白にする
- `HOLIDAY_RED` / `SATURDAY_BLUE` を `var(--sun)` / `var(--sat)` に変え、
  暗い方に `#CB7D75` / `#779ABA` を置いた。**日曜の赤は 2.82 → 5.86**
- 暗い方の地を冷たい側へ。`bg #0B0D10` / `card #12151A` / `line #1F252D` /
  `ink #E7EBF0` / `mut #8A939F`

確かめたこと。

- 明るい方は**書いた値が公開中と完全に一致**（マクド `rgb(168,200,172)`、
  花火 `rgb(224,180,154)`、歯医者 `rgba(211,204,228,.72)`、返却 `rgba(211,211,211,.72)`）
- **測るときの注意**：プレビューの画面は `document.hidden` なので CSS の遷移が
  進まず、`getComputedStyle` は遷移の**開始側の色**を返す。`el.style` の
  「書いた値」を見ること。ここで一度、明るい方が壊れたと誤って読んだ
- 色そのものは替えていないので、`OLD_DEFAULT_COLORS` の移行もウィジェットの
  作り直しも要らない（それが要るのは B）

### B. 色そのものを Lab の並びに替える（そのあと）
- **明るい方も暗い方も、両方まとめて。** 片方だけ替えると2つの配色が別の家族になる
- ウィジェットと共有画像も一緒
- 得られるのは数字（1.45 → 1.48）より「揃って見えること」
- **落とし穴：`OLD_DEFAULT_COLORS` に、いまの4色を足すこと。**
  いま載っているのはさらに前の版の色（`#534AB7` など）なので、
  足さないと **既に使っている人には新しい色が届かない**

以下、Bで入れる値。**2026-08-22 決定。**`tools/theme-global.cjs` の案でいく。線は消さない
（`tools/theme-light.cjs` の「線を全部消す」案は採らない）。

### 決めた理由
- 4つの種類の色を **Lab で明るさと鮮やかさを揃えた**（ばらつき 1.00 倍）。
  いまの紫・緑・オレンジ・グレーは明るさがばらばらで、集めた色に見えていた
- **その他だけ鮮やかさを落とす**（C 40 → 12）。「種類が無い」ことを色の弱さで表す
- **今日はどの種類とも 60度以上離す**（205度）。今日は予定ではないので、
  種類と同じ色を使うと嘘になる
- ピルは「刷ったもの」ではなく **「光っているもの」**。確定＝薄い面＋明るい縁、
  まだ＝縁だけ＋点線。**面の有無と線の切れ目の2つ**で差をつける

### 値（そのまま入れる）

暗い方の地：`bg #0B0D10` / `cell #12151A` / `line #1F252D` / `line-faint #171C22`
　　　　　　`ink #E7EBF0` / `mut #8A939F` / `faint #565F6B`
　　　　　　面の濃さ 24%、縁の濃さ 85%、角丸 6
　　　　　　**→ この地と面の濃さは A で先に入れた（v0.29.0）。B で残るのは
　　　　　　種類の4色と、明るい方の地。角丸だけまだ 4 のまま**

| | 色 | 塗りの面 | 塗りの字 | まだの字 |
|---|---|---|---|---|
| 用事 | `#8997df` | `#2f3449` | `#e5e8f8` | `#f1f3fb` |
| バイト | `#4cac84` | `#203933` | `#d8ede4` | `#eaf5f0` |
| 遊び | `#d9856d` | `#42302e` | `#f7e4df` | `#faf0ed` |
| その他 | `#889eaf` | `#2e363e` | `#e5eaed` | `#f1f3f5` |

今日 `#00c5d1`　日曜 `#dc7a78`　土曜 `#509dcf`

---

明るい方の地：`bg #FAFBFC` / `cell #FFFFFF` / `line #E4E8ED` / `line-faint #EFF2F5`
　　　　　　　`ink #10141A` / `mut #69727E` / `faint #A3ABB5`
　　　　　　　面の濃さ 30%、縁の濃さ 100%、角丸 6

| | 色 | 塗りの面 | 塗りの字 | まだの字 |
|---|---|---|---|---|
| 用事 | `#5a6aa9` | `#ced2e5` | `#323a5d` | `#171b2a` |
| バイト | `#1b7c58` | `#bbd8cd` | `#0f4430` | `#071f16` |
| 遊び | `#a35944` | `#e3cdc7` | `#5a3125` | `#291611` |
| その他 | `#5b707f` | `#ced4d9` | `#323e46` | `#171c20` |

今日 `#00838d`　日曜 `#b24c4f`　土曜 `#0074a7`

### 測った値（入れたあと、ここから落ちていないか確かめる）
- **塗りとまだの面の差**：暗い方 1.48〜1.49／明るい方 1.50〜1.52
  （いまの明るい画面は 1.40。**このアプリの核は少し強くなる**）
- **面の上の字**：暗い方 10.1／明るい方 7.3
- **まだの字**：暗い方 16.3〜16.5／明るい方 17.1〜17.3

### 入れるときに気をつけること
- **ウィジェットと共有画像も一緒に変える。** カレンダー本体だけ変えると、
  ホーム画面のウィジェットが古い色のままで食い違う。
  `KimatteruWidget.swift` と `sharecard.js` の両方
- **サポーターカードは別系統。** あれは紙と箔の色で、種類の色ではない。触らない
- 気づいている小さな点：**まだのピルは、字より縁のほうが色を持っている。**
  縁が種類を示すので実用上は困らないが、狙ってそうしたわけではない。
  実機で見て気になったら、まだの字にもう少し色を寄せる

## 3. App Store のページ

`tools/hero2.cjs` に案がある。

- **1枚目＝案A**（二つ並べる）。掴む役。「二つある」だけを大きく確実に
- **2枚目＝案B**（点線 → どうなった？ → 塗り）。説明する役。このアプリの正体
- 3枚目以降 — バイトのまとめ、ウィジェット、空き状況

見出しの語に**実線と点線を引く**（「決まった」に実線、「まだ」に点線）。
宣伝の文字そのものがアプリの決まりを説明している状態にする。

### 直しかけ
- 案Bの下、カレンダーが週の途中で切れていて「切り忘れ」に見える。
  もっと大胆に切るか、切らないかに寄せる
- 英語のコピーがまだ弱い。日本語版のほうが強い。英語を母語とする人に
  見てもらう価値がある

---

## 4. 世界に出すなら（順番を守る）

**これは配色の話ではなく、作り直しの話。** 次の版で片づく規模ではない。

1. **未確定の考えが単独で立つか決める。**
   世界に通じる部分（未確定）と、日本にしか通じない部分（バイト・給料・扶養）が
   同じアプリの中で混ざっている。時給で働く人は世界中にいるが、扶養の壁は
   日本の制度。バイトを使わない人にとってもこのアプリが何のためにあるか
   分かるか、を先に決める
2. **応援の金額を実額で記録する。**
   いまは `src/tipjar.js` の `TIPS` に円で固定してある。$1.99 を払っても
   「¥300 応援した」と記録される。サポーターカードの合計も段もこの数字で
   決まるので、直さずに広げると嘘の記録が残る
3. **月表示が英語で持たない。**
   マスの幅 53.6px に英語は6文字しか入らない。`Seminar` も `Fireworks` も
   切れる。配色では解決しない、情報設計の問題。
   A 時刻と点だけにする／B 帯は残して切る／C 週表示か一覧を主役にする
4. 翻訳、祝日データ、週の始まり
5. 配信国を広げる

**3番を飛ばして5番をやると、本番で不具合が出る。** 順番を守る。

---

## 5. 保留にしたもの（やらないと決めた、ではない）

- **カードの地紋**（模様を敷く案）— 取り下げ。求められていたのは柄ではなく質感だった
- **予定に記録を書く欄**（日記のようなもの）— **見る場所を作るのが先。**
  書く欄だけ置くと、書いたものが行方不明になる。まとめカードに
  「今月やったこと」の置き場ができてから
- **.ics の取り込み**（取り込みの第2段）— 手つかず
- **応援の見返り**（要望を送る道など）— 判断していない。
  消耗型で機能を解放してはいけないので、やるなら非消耗型を別に作る

---

## 参考：提出のときに毎回やること

失敗した2つを繰り返さないための手順。詳しくは記憶にも残してある。

1. `node tools/asc.mjs build` — いちばん新しいビルドを付ける
   （画面でやるなら**右上の「保存」を押すまで確定しない**）
2. `node tools/asc.mjs notes` — 審査メモに応援への行き方を足す
3. `node tools/asc.mjs status` — 5つ全部に ✓ が付くまで出さない
4. 提出後にもう一度 `status`。**提出の箱が1つ・中身4件**（本体＋課金3つ）で、
   課金が `WAITING_FOR_REVIEW` になっているか。3件と1件に分かれていたら失敗
