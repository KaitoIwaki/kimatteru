import { endMoment } from './whenlib';
// ベルに溜まるお知らせ。
// 種類は2つだけ ——「シフトの記録を促すもの」と「アップデートのお知らせ」。

export const KIND_SHIFT = 'shift';
export const KIND_INFO = 'info';

// アップデートのお知らせ。新しい版を出すときは、ここに上から足していく。
// version はそのお知らせを出し始めた版。初回起動の人には出さない。
export const RELEASE_NOTES = [
  {
    version: '0.22.1',
    title: 'サポーターカードに段ができました',
    body: '応援してくださった合計に応じて、カードの紙と箔が変わります。ノーマル（生成りの紙に真鍮）、¥1,000 からゴールド、¥3,000 からブラックの3段です。カードの下に、次の段まであといくらかを出しています。段が上がっても機能は増えません。見た目だけです。カードは指でなぞると、なぞった分だけその場で回ります。途中で手を離せば近いほうの面に落ち着き、勢いよく払えば向こうまで回ります。押して裏返すこともできます。',
  },
  {
    version: '0.20.0',
    title: 'サポーターカードを作りました',
    body: '応援してくださった方に、サポーターカードをお渡しします。設定の応援の欄から開くと、いつから支えてくださっているかが入った1枚のカードが出ます。タップで裏返すと、これまでの記録が見られます。画像にして共有することもできます。名前を入れられますが、その名前も端末の中だけに保存されます。',
  },
  {
    version: '0.19.0',
    title: '画面の地を白くしました',
    body: 'ベージュだった地を白に変えて、文字を読みやすくしました。予定の色は変えていません（色で種類を見分けるのは、このアプリの要なので）。設定のいちばん下に「開発を応援する」を置きました。応援しても機能は増えません。広告なし・通信なしのまま作りつづけるための受け皿です。',
  },
  {
    version: '0.18.0',
    title: 'くり返しと複数日を作り直しました',
    body: 'くり返しに毎月・毎年が増え、毎週は曜日をえらべるようになりました（「毎週 火・木」など）。同じ予定を別々の日にまとめて置きたいときは、予定を作る画面の「＋」から「複数日」を足してください。日にちをえらぶ画面は、押した日だけが選ばれるように変えています（前は2回目から追加になっていて、押し間違いを直せませんでした）。深夜のシフトのように日をまたぐ予定は、終わりが翌日だと画面に出るようにし、記録の催促が始まる前に届いてしまう問題も直しました。',
  },
  {
    version: '0.17.0',
    title: '控えをファイルから戻せるようになりました',
    body: '設定の「控えから戻す」を押すと、書き出した控えのファイルをそのままえらべるようになりました。控えを開いて中身を全部コピーして貼り付ける、という手順は要りません。うまくえらべないときのために、貼り付けで戻すやり方も残してあります。戻したあとは、何件戻したかを画面に出します。',
  },
  {
    version: '0.16.0',
    title: '予定が消えないようにしました',
    body: '予定の保存を二重にして、端末の都合で消えてしまわないようにしました。保存できなかったときは画面でお知らせします。新しい予定の種類は「用事」から始まるようにし、種類ごとに時間帯の既定を持たせました（バイトなら17時から）。あわせて、はじめて開いたときの案内を作り直しました。4枚とも、紙に字が現れる形にしています。1枚目は点線の予定をタップして「確定した」を押さないと先へ進めません（このアプリで一番覚えてほしい操作なので、本物と同じ手順にしています）。2枚目で空き状況、3枚目で「予定を見せずに空いてる日だけ送れる」ことを紹介します。時給を聞く画面はやめました（時給はバイト先を作るときに決めます）。まだ予定が1件も無いときの案内が、カレンダーの最終週を隠していたのも直しています。',
  },
  {
    version: '0.15.0',
    title: 'くり返し・場所・メモを足しました',
    body: '毎日や毎週の予定を、まとめて置けるようになりました。予定に場所を入れておくと、開いたときに地図で開けます。メモには持ち物を書けます。この3つは予定を作る画面の「＋」から足すので、ふだんの画面は短いままです。カレンダーの年月をタップすると、離れた月へ一度に飛べます。「空いてる日をシェア」は、空き状況の画面に移しました。規約とプライバシーポリシーも、いまの中身に合わせて書き直しています。',
  },
  {
    version: '0.14.0',
    title: '予定の控えを取れるようになりました',
    body: '設定の「控え」から、予定をまるごとファイルに書き出せます。機種変更や紛失にそなえて、ときどき取っておいてください。書き出した控えから戻すこともできます。',
  },
  {
    version: '0.13.0',
    title: '前後の月の日付が見えるようになりました',
    body: '月のはじめと終わりの空いていたマスに、前後の月の日付を薄く出すようにしました。まとめの数字は少し細くして、設定からお問い合わせできるようにしています。',
  },
  {
    version: '0.12.2',
    title: 'カレンダーの見た目を整えました',
    body: '前後の月のマスに敷いていた色をやめて、月の形がすっきり出るようにしました。1日に置ける予定を3件から4件に増やしています。曜日の見出しも、表の一部として並べ直しました。',
  },
  {
    version: '0.12.1',
    title: 'カレンダーを画面いっぱいに広げました',
    body: '月表示の外枠をなくして、画面の横いっぱいまで使うようにしました。予定の名前が6文字まで入るようになっています。曜日の見出しとマスが少しずれていたのも直しました。',
  },
  {
    version: '0.12.0',
    title: '休憩を引いて給料を出せます',
    body: '働いた時間を記録するとき、休憩が何分あったかをえらべるようになりました。休憩が時給に入らないバイト先では、これまで実際より多めに出ていました。えらぶと、引いたあとの実働時間がその場に出ます。',
  },
  {
    version: '0.11.2',
    title: '空き状況も横に払って月を送れます',
    body: '「いつ空いてる？」の一覧を左右に払うと、月が変わるようになりました。カレンダーの予定の帯を一段細くして、その日に何が入っているかを見渡しやすくしています。',
  },
  {
    version: '0.11.1',
    title: '規約とプライバシーポリシーを更新しました',
    body: '予定ごとのお知らせを足したので、通知についての説明を書き直しました。給料計算が休憩時間を差し引かないことも、はっきり書いています。長い文章が読みやすくなるよう、行末をそろえました。',
  },
  {
    version: '0.11.0',
    title: '設定を整理して、種類を足せるようにしました',
    body: '設定画面を短くしました。時給はバイト先ごとに決めるようにして、設定からは外しています。予定の種類は、設定から名前を変えたり新しく足したりできます。iPhone のカレンダーから祝日を取り込んでしまう問題も直しました。',
  },
  {
    version: '0.10.0',
    title: 'お知らせと、スワイプで削除ができます',
    body: '予定ごとに「30分前に知らせて」を設定できるようになりました。10分前から1週間前までえらべます。予定の一覧は左にスワイプすると削除できます。予定を作る画面も、必要なところだけ開く形にして短くしました。時刻は5分きざみでえらべます。',
  },
  {
    version: '0.9.0',
    title: '何日か続く予定を、1本の帯で置けます',
    body: '合宿や旅行のように何日か続く予定は、まとめて1本の帯になりました。終日をえらぶと「何日間」が出ます。カレンダーの線を減らして、予定の名前も長く出るようにしています。',
  },
  {
    version: '0.5.0',
    title: '祝日と、お知らせの一覧を追加しました',
    body: 'カレンダーの祝日が赤くなりました。このベルからは、記録し忘れているシフトやアップデートのお知らせをまとめて見られます。',
  },
  {
    version: '0.3.0',
    title: 'iPhone のカレンダーから取り込めます',
    body: '設定の「予定の取り込み」から、いま使っているカレンダーの予定をまとめて持ってこられます。読むだけで、書き込みはしません。',
  },
];

export const shiftNoticeId = (eventId) => 'shift-' + eventId;
export const infoNoticeId = (version) => 'info-' + version;

/**
 * 終わったのにまだ実績を入れていないバイトについて、お知らせを作る。
 * すでに同じものがあれば作らない。記録済みになったものは取り除く。
 */
export function syncShiftNotices(notices, events, now) {
  // 「終わったのにまだ記録していない」バイト。
  // 年月日を1段ずつ比べていたが、それだと 22:00–1:00 の深夜勤務で
  // 「1:00 はもう過ぎている」と判定され、シフトが始まる前の朝に催促が出ていた。
  // 終わる瞬間を出してから、いまと比べる。
  const due = events.filter((e) => {
    if (e.type !== 'baito' || e.status !== 'kakutei' || e.allDay) return false;
    return endMoment(e).getTime() <= now.getTime();
  });

  const dueIds = new Set(due.map((e) => shiftNoticeId(e.id)));
  // 記録済み・削除済みのシフトのお知らせは消す
  let out = notices.filter((n) => n.kind !== KIND_SHIFT || dueIds.has(n.id));

  for (const e of due) {
    const id = shiftNoticeId(e.id);
    if (out.some((n) => n.id === id)) continue;
    out.push({
      id,
      kind: KIND_SHIFT,
      eventId: e.id,
      title: `${e.title}、おつかれさま`,
      body: `実働時間はどうでしたか？タップして記録しよう（${e.start}–${e.end}）`,
      at: endMoment(e).getTime(),
      read: false,
    });
  }
  return out;
}

/**
 * 版の新しい・古いを数として比べる。
 * 文字列のまま比べると '0.10.0' < '0.9.0' になってしまい、
 * 0.10 以降のお知らせが誰にも出なくなる。
 */
export function cmpVersion(a, b) {
  const pa = String(a).split('.').map(Number);
  const pb = String(b).split('.').map(Number);
  for (let i = 0; i < 3; i++) {
    const x = pa[i] || 0, y = pb[i] || 0;
    if (x !== y) return x < y ? -1 : 1;
  }
  return 0;
}

/** 版が上がったときに、アップデートのお知らせを足す */
export function syncInfoNotices(notices, currentVersion, lastSeenVersion) {
  // 初めて使う人には、過去のお知らせを出さない
  if (!lastSeenVersion) return notices;
  if (lastSeenVersion === currentVersion) return notices;
  const out = [...notices];
  for (const r of RELEASE_NOTES) {
    if (cmpVersion(r.version, lastSeenVersion) <= 0) continue;
    // まだ配っていない版のお知らせは出さない（書きかけを取り違えないように）
    if (cmpVersion(r.version, currentVersion) > 0) continue;
    const id = infoNoticeId(r.version);
    if (out.some((n) => n.id === id)) continue;
    out.push({ id, kind: KIND_INFO, title: r.title, body: r.body, at: Date.now(), read: false });
  }
  return out;
}

export const unreadCount = (notices) => notices.filter((n) => !n.read).length;

/** 新しい順に並べる */
export const sortNotices = (notices) => [...notices].sort((a, b) => b.at - a.at);

// 「たった今」「3時間前」のような言い方にする
export function relativeTime(ms, now) {
  const diff = Math.max(0, now - ms);
  const min = Math.floor(diff / 60000);
  if (min < 1) return 'たった今';
  if (min < 60) return min + '分前';
  const hour = Math.floor(min / 60);
  if (hour < 24) return hour + '時間前';
  const day = Math.floor(hour / 24);
  if (day < 7) return day + '日前';
  const d = new Date(ms);
  return d.getMonth() + 1 + '月' + d.getDate() + '日';
}
